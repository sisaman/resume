\usepackage[T1]{fontenc}

% LaTeX will typeset using Computer Modern Roman, which a lot of
% non-mathematicians and non-engineers won't like. Also, a few PDF
% viewers may not render CMR very well. Instead, Times New Roman can
% be used. That's what this package does.
\usepackage{times}
\usepackage[dvipsnames]{xcolor}
\usepackage{fontawesome}
\usepackage[shortlabels]{enumitem}
\usepackage{color,hyperref}
\usepackage{tabularray}
\usepackage{graphicx}
% The automated optical recognition software used to digitize resume
% information works best with fonts that do not have serifs. This
% command uses a sans serif font throughout. Uncomment both lines (or at
% least the second) to restore a Roman font (i.e., a font with serifs).
% (NOTE: This requires the times package above)
% \renewcommand{\familydefault}{\sfdefault}


%% Use these lines for letter-sized paper
\usepackage[paper=a4paper,
            %includefoot, % Uncomment to put page number above margin
            % marginparwidth=1in,     % Length of section titles
            % marginparsep=.05in,       % Space between titles and text
            margin=0.5in,               % 1 inch margins
            % top=0.5in,
            % includemp
            ]{geometry}

\setlength{\parindent}{0in} % Get rid of indenting throughout entire document
\definecolor{darkblue}{rgb}{0.0,0.0,0.3}
\hypersetup{colorlinks,breaklinks,
            linkcolor=darkblue,urlcolor=darkblue,
            anchorcolor=darkblue,citecolor=darkblue}
%%%%%%%%%%%%%%%%%%%%%%%% End Document Setup %%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%% SECTION HEADINGS

% The section headings. Flush left in small caps down pseudo-margin.
%
% Usage: \section{section name}
\renewcommand{\section}[1]{
	\medskip % Space before section
    \textsc{\large \textcolor{Blue}{#1}} % Navy blue, bold, italic section title
    \medskip % Space before horizontal line
    {\hrule}
	% \medskip
}

% An itemize-style list with little space between items
\newlist{innerlist}{itemize}{3}
    \setlist[innerlist]{label=\enskip\textbullet,leftmargin=*,parsep=0pt,itemsep=0pt,topsep=0pt,partopsep=0pt}

\newlist{outerlist}{itemize}{3}
	\setlist[outerlist]{leftmargin=0pt, label={}}


\def \name#1{\def\@name{#1}}
\def \@name {}

\def \email#1{\def\@email{#1}}
\def \@email {}

\def \phone#1{\def\@phone{#1}}
\def \@phone {}

\def \location#1{\def\@location{#1}}
\def \@location {}

\def \website#1{\def\@website{#1}}
\def \@website {}

\def \linkedin#1{\def\@linkedin{#1}}
\def \@linkedin {}

\renewcommand{\maketitle}{

  \begin{tblr}{lX[r]}
    {\Huge\bf\@name} & \SetCell[r=5]{r} \includegraphics[width=0.18\textwidth]{sina-sajadmanesh-photo.png} \\
    \faLinkedinSquare~~~\href{https://www.linkedin.com/in/\@linkedin}{linkedin.com/in/\@linkedin} & \\ 
    \faEnvelope~~~\href{mailto:\@email}{\@email} & \\
    \faPhone~~~\@phone & \\
    \faLocationArrow~~~\@location & \\
  \end{tblr}

  % \begin{wrapfigure}[4]{r}{0.2\textwidth}
  %   \vspace{-0.5cm}
  %   \includegraphics[width=0.17\textwidth]{sina-sajadmanesh-photo.jpg}
  % \end{wrapfigure}
  % {\Huge\bf\@name} % Set the name size, bold, and italic style
  % \medskip

  %   \faLinkedinSquare~~~\href{https://www.linkedin.com/in/\@linkedin}{linkedin.com/in/\@linkedin}\\
  %   \faEnvelope~~~\href{mailto:\@email}{\@email}\\
  %   \faPhone~~~\@phone \\
  %   \faLocationArrow~~~\@location    \\
  \medskip
}